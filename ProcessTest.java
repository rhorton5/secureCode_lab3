import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.lang.Runtime;
import java.lang.invoke.MethodHandles;
public class ProcessTest {
    public static void main(String [] args) throws IOException, InterruptedException{
        /*Runtime rt = Runtime.getRuntime();
        Process p = rt.exec("java Quine");
        BufferedReader ibr = new BufferedReader(new InputStreamReader(p.getInputStream()));
        BufferedReader obr = new BufferedReader(new InputStreamReader(p.getErrorStream()));
        System.out.println("INPUT: " + ibr.readLine());
        System.out.println("OUTPUT:" + obr.readLine());
        p.waitFor();

        ibr.close();
        obr.close();*/

        char [] a = {'.','j','a','v','a'};
        System.out.println(
    }
}
